<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="GearScout Installer"
    Width="780" Height="660"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    AllowsTransparency="True"
    ResizeMode="NoResize"
    Background="Transparent">

    <Window.Resources>
        <!-- Obsidian palette, matches GearScout/Skin.lua Skin.OBSIDIAN exactly. -->
        <SolidColorBrush x:Key="BgBrush"       Color="#F707080A"/>
        <SolidColorBrush x:Key="PanelBrush"    Color="#FF0C0E11"/>
        <SolidColorBrush x:Key="RaisedBrush"   Color="#FF14171C"/>
        <SolidColorBrush x:Key="HoverBrush"    Color="#FF1D2128"/>
        <SolidColorBrush x:Key="LineBrush"     Color="#0DFFFFFF"/>
        <SolidColorBrush x:Key="LineHardBrush" Color="#1CFFFFFF"/>
        <SolidColorBrush x:Key="TextBrush"     Color="#FFE6EAF2"/>
        <SolidColorBrush x:Key="DimBrush"      Color="#FF828B9C"/>
        <SolidColorBrush x:Key="AccentBrush"   Color="#FF4798F6"/>
        <SolidColorBrush x:Key="GoodBrush"     Color="#FF3EC463"/>
        <SolidColorBrush x:Key="WarnBrush"     Color="#FFF6B12B"/>
        <SolidColorBrush x:Key="BadBrush"      Color="#FFEF5851"/>

        <!-- Flat rounded panels only. No gradients, no drop shadows, no gloss. -->
        <Style x:Key="RoundedPanel" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="{StaticResource RaisedBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineHardBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="DimText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource DimBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- Secondary button: raised flat fill, hairline border, hover lightens the fill. -->
        <Style x:Key="BtnSecondary" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource RaisedBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineHardBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="6" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary button: single blue accent fill, used for the one action that matters. -->
        <Style x:Key="BtnPrimary" TargetType="Button" BasedOn="{StaticResource BtnSecondary}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#FF08101C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="6" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                Opacity="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat title bar close button, just an "x", no icon assets needed. -->
        <Style x:Key="BtnClose" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource DimBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="0,8,0,0" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BadBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom flat checkbox: a small rounded box plus label, no OS chrome. -->
        <Style x:Key="ObsidianCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Box" Width="16" Height="16" CornerRadius="3"
                                    BorderBrush="{StaticResource LineHardBrush}" BorderThickness="1"
                                    Background="{StaticResource PanelBrush}" VerticalAlignment="Center">
                                <Path x:Name="Check" Data="M2,7 L6,11 L14,2" Stroke="{StaticResource AccentBrush}"
                                      StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Plain content presenter for list items, no default selection chrome. Selection
             is painted by hand on the item's own Border in code-behind instead. -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="{StaticResource BgBrush}"
            BorderBrush="{StaticResource LineHardBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom title bar, draggable, no OS chrome. -->
            <Border x:Name="TitleBar" Grid.Row="0" CornerRadius="10,10,0,0" Background="{StaticResource RaisedBrush}" Height="40">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="GearScout Installer" Foreground="{StaticResource TextBrush}"
                               FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0"/>
                    <Button x:Name="CloseButton" Grid.Column="1" Style="{StaticResource BtnClose}" Content="x"/>
                </Grid>
            </Border>

            <!-- Main content. -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,18,24,20">

                <TextBlock Style="{StaticResource DimText}" Margin="0,0,0,16"
                    Text="This installs GearScout into your World of Warcraft AddOns folder. Pick an installation below, then click Install."/>

                <TextBlock Style="{StaticResource SectionLabel}" Text="World of Warcraft installations found"/>
                <Border Style="{StaticResource RoundedPanel}" Padding="10" Margin="0,0,0,14">
                    <ListBox x:Name="InstallList" Background="Transparent" BorderThickness="0"
                             MaxHeight="230" ScrollViewer.CanContentScroll="False"/>
                </Border>

                <Button x:Name="BrowseButton" Style="{StaticResource BtnSecondary}" HorizontalAlignment="Left"
                        Content="Browse for a different folder..." Margin="0,0,0,18"/>

                <TextBlock Style="{StaticResource SectionLabel}" Text="Options"/>
                <CheckBox x:Name="LeadCheckBox" Style="{StaticResource ObsidianCheckBox}"
                          Content="Also install GearScout_Lead" IsChecked="False" Margin="0,0,0,4"/>
                <TextBlock x:Name="LeadExplainText" Style="{StaticResource DimText}" Margin="24,0,0,18"
                    Text="Officer only. Lets you see other players' gear from your raid lead console. Most people do not need this."/>

                <Border Style="{StaticResource RoundedPanel}" Padding="14" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock x:Name="SourceText" Style="{StaticResource DimText}" Text="Checking for an install package..."/>
                        <TextBlock x:Name="SummaryText" Style="{StaticResource DimText}" Margin="0,10,0,0" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                    </StackPanel>
                </Border>

                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="InstallButton" Grid.Column="0" Style="{StaticResource BtnPrimary}" Content="Install"/>
                    <Button x:Name="RescanButton" Grid.Column="1" Style="{StaticResource BtnSecondary}" Content="Scan again" Margin="10,0,0,0"/>
                </Grid>

                <TextBlock x:Name="StatusText" Style="{StaticResource DimText}" TextWrapping="Wrap"/>

            </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
