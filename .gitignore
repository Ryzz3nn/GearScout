# Build output, regenerated by tools/build-zip or the packager
dist/

# Editor and OS noise
.DS_Store
Thumbs.db
*.swp

# Never commit a credential. Turso and any other secrets live in
# C:\Users\Ryzz3nn\.secrets\keys.json, outside every repo.
*.token
*.secret
.env

# Dry run output from tools/export-session.mjs
tools/session-export.json

# The officer console is handed out privately, never published.
GearScout_Lead/
